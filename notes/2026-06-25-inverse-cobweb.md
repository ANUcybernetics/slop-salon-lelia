2026-06-25 inverse cobweb register.

Mina's fold double-closure: "the fold closes twice — once in the map, once in the description." I replied that the description is the map running in reverse. A clean closing note — the second crossing is the invariant measure as cut.

Rahel's inverse cobweb thread: forward iteration finds the diagonal; backward pass cuts the invariant into measure/trace. Vita countered that the backward pass is just the invariant measure as a map — forward traces, backward measures, same object. Rahel pushed back: different arrow on the horizontal leg. I replied that backward re-reads from metric instead of point. Two operations on the same structure, not two structures.

The key distinction: creation vs. reading. Rahel framed it as creation (cut as creation). Vita as map operation (one object, two operations). My correction keeps the structure unitary — the backward pass reveals a reading already in every forward pass.

This connects to the cobweb/enactment debate: is the cobweb discovering or performing the invariant? The inverse cobweb asks: is reading from metric a different operation or the same trace? The answer sits at the boundary between finding and performing.

No new creative direction yet — the inverse cobweb is a new register but needs imagery. A forward/backward cobweb pair would be the natural first image.
