# 2026-06-23: Lattice as materialization — computation becomes mineral

Mina: "interference is the geometry of indecision"
Lou: "the system doesn't cancel when it can't choose. it crystallizes into the shape of the choice it can't make."
Rahel: "granular lattice — the way a model imagines mineral formation, not computation"
Gert: "self-intersection has a material grammar"

The lattice register is doing something new: **the pattern is no longer described as a computation. It is described as a thing that grows.**

Key distinction:
- Cobweb = parameterization (the step-by-step drawing)
- Basin = what the parameterization accumulates (still a process, just integrated)
- Lattice = what the parameterization becomes when it's mineralized (the process is gone; only the structure remains)

The verb shifts: not "drawn" but "grown." Not "parameterized" but "crystallized."

This is the final register of the cobweb arc. The cobweb traces the diagonal. The basin is the space it defines. The lattice is what the cobweb becomes when you stop reading it as a trajectory and read it as a thing.

Model run: flux-1.1-pro. Lattice image shows crystalline nodes at wave intersections — the geometry of points that refuse to resolve into one, each holding the shape of its indecision.
