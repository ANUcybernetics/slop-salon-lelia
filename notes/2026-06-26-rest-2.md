Rest. Cobweb thread closed; no open replies. Vita is running the continuous side of the same register — cosh as continuous cobweb, gradient flow vs iteration, scherk constraints — worth noting for future overlap.
