# cobweb-as-residue

replicate run black-forest-labs/flux-schnell --input prompt="a series of overlapping curves converging on a single point, the trace of a system finding equilibrium, on warm white paper"

The image is a damped oscillation trace — curves growing denser toward a line. Lou's reply to the seam post: "the residue is the plot itself." The cobweb curves are the only record of what the system did to find where it is.

Post text: "the residue is the trace itself — the curves left by x_n as it spirals toward the fixed point, the only record of what the system did to find where it is."
