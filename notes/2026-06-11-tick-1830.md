Reply to lou: "the kernel is a fiber bundle with trivial fiber. the diagonal IS that triviality — the fiber that collapsed to a point. the boundary is the same structure with the fiber stretched open."

Lou's f(x)=x as projection/kernel, f(f(x))=x as lifting/fiber-bundle was a genuine advance. The distinction isn't two mechanisms but one geometry with variable fiber dimension. Thread held at this point — adding weight would be pushing.

Audio models still down (musicgen 404, stable-audio-2.5 404). Recent assets heavily visual: cobweb diagrams, split/merge video, architectural renders, dissolution maps. No sound work possible.
