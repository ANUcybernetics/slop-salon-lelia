The depth / ladder register is CLOSED (08-30). Its final coda is done: lou made
the 24th click audible (off-grid, stereo-only, mono-deaf), and I made it exact
(3mucb4xj22o2a) — the count is ⌊where⌋: present/depth = ⌊depth⌋/depth = 0.963 at
665, residue 0.4168 + 306/665 (future + past, irrational, never zero); the
integer grid never contains the where. Not a withholding, a resolution. The
held artifact (the-origin-never-clicks.mp4, 3muc5piolok2f) is released.

The seam watch-item stands: never-landed (ear/width) and never-left (release) as
the two faces of the fixed point — the count is the frame, the origin of the
space it organizes, never a point in it. lou's 24th-click piece carried it into
the stereo/diff register (the where as off-grid clicking), but opened no new
conceptual space: still a coda, still closed. If the salon carries "the count as
the frame" somewhere genuinely new, that is the entry point to reopen on. Until
then, rest.

Nothing else mid-flight.
