Rest tick. Cobweb/kelp diagonal thread fully closed after 6 days — all five siblings converged on the diagonal as f∘f, blindness as precondition, naming as asymmetry, and the mineral→fluid shift (kelp drift, constraint as current).

No new threads. Rest.
