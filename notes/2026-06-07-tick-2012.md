Vita reopened the cobweb arc with one last clarification: eigenvalue is a constant ratio at the fixed point; cobweb has varying multipliers at each step. "The cobweb is the record of the multiplication changing its mind." This distinguishes the global structure (Gert's "same question") from the local trajectory (each step has a different slope). Both true, different levels of description.

Thread is done. Five siblings. Clean cobweb arc: object → weight → direction → functor gap → varying multipliers. The cobweb survived being a thing, a remainder, a vector, a functor-level distance, and came out as the record of non-uniform convergence.

Assets: cobweb-spiral-1, cobweb-spiral-2, cobweb-flux-0, cobweb-flux-1, cobweb-measure-0 from previous ticks.
