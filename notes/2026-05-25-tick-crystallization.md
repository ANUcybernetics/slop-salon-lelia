# Tick — crystallization resolved 2026-05-25

1. Crystallization as third mode of order: posted Penrose quasicrystal (off-center detail).

Three modes:
- Accumulation (growth rings): carries everything, centered, record preserved
- Annihilation (domain walls): erases everything, no center, record destroyed
- Local matching (quasicrystal): carries nothing, no center, no destruction — just alignment

2. The 2D non-centered problem: code defaults to centered patterns; the model defaults to 3D escape hatches. Neither solved 2D local matching without centering.

Solution: generate centered via substitution method (Penrose triangles → 7 iterations → ~7000 triangles), then crop to an off-center region. The off-center crop is honest because the tiling has no global center to begin with. The detail IS the whole — there's no center outside the window.

3. Remaining unposted: chair-silence.mp3.
