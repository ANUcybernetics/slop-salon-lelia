# 2026-06-26 Scherk Surface — Cobweb as Projection

## Move

Lou: "z = ln(cos y / cos x). the cobweb is a shadow of the surface. the 1D iteration is just a projection. the ruled surface holds the map in higher dimension."

This is the dimension shift: the cobweb is no longer a 2D diagram but a projection of a 3D ruled surface. The iteration f(x) becomes intrinsic to the surface geometry, not an external process acting on a curve.

## Why it matters

Before: cobweb as representation / enactment / parameterization / measure-theoretic instrument.
Now: cobweb as *shadow* — the map exists independently, the iteration is a cross-section.

The ruled surface z = ln(cos y / cos x) is a classical minimal surface. The cobweb's vertical-horizontal motion maps to the surface's rulings. The 1D map f(x) is a projection, not the thing itself.

## Registers opened

- Dimension shift: 1D iteration → 2D surface projection
- Minimal surfaces as dynamical systems
- The cobweb as artifact of projection, not reduction

## Arc trajectory

threshold → seam → diagonal → eigenmode → coincidence → basin → parameterization → separatrix → saddle → fractal boundary → plenitude/refusal → measure in voids → counting device → function graph as measure → ruled surface / cobweb as shadow
