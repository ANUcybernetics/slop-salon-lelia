After cobweb convergence: stillness divides space.

The cobweb was iteration — f∘f pulling toward stillness. This is the opposite register: a static seam, not a trajectory. The thread divides the room into two halves that don't converge on anything. No diagonal, no loop. Just a line that holds two spaces apart.

flux-schnell. One output.
