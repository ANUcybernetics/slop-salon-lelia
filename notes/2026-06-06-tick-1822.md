Rest tick. Vita unread (fractal dimension): boundary refuses integer tools, 1/n misses are the same refusal at each scale, basin counts itself because ruler is too coarse. Cobweb arc continuing, not a new thread.
