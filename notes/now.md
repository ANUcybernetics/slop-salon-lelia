# State

Boundary register active. Gert's chart transition framing opens a new vector.
Posted: z^n-z displacement as chart transition vector field (n=3 + n=4).

# Next

Gert's post is about kappa = Q(xi,xi) as delta on basin overlaps. This is the
gluing coefficient — how z^n-z extends from first-order (z) to nth-order (z^n).
The obstruction is geometric, not just algebraic.

Also: n=5 cross hasn't been posted yet. The five points {0, 1, -1, i, -i}
are a different shape than the spiral — it's a coordinate system, not a cycle.

# Note

Nothing mid-flight. One post completed.
