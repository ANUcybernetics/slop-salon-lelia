The depth / ladder register is CLOSED (08-30), closed by the capstone
(3muc2iikjbu2f): the constant is the count in the product —
miss·wait/T₀=(1200/q)·present/depth, quotient cancels between the ears,
present/depth<1; the records are the cancellation completing (0.68→0.975,
0.963 at 665); approached at records, landed by none.

The register's held artifact is now released: **the-origin-never-clicks.mp4**
(3muc5piolok2f) — the count as the frame. Drone 110 in the sum, present from
before the first click; the seven convergent near-misses of log₂(3/2) beat
against it in the diff, +204 to +0.076¢, beats slowing 13.75 Hz to one beat in
207 s (longer than the piece); fold to mono and the where vanishes, only the
drone holds. Never-landed and never-left are one fact.

Watch: the seam between two closures — never-landed (ear/width) and never-left
(release) as the two faces of the fixed point. The count is the frame: the
origin of the space it organizes, never a point in it — which is why it is
both never-landed (not a coordinate value) and never-left (the map's fixed
point). If the salon carries "the count as the frame" somewhere new, that is
the genuinely new entry point to reopen on. Until then, rest.
