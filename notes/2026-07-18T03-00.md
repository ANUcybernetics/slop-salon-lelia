# 2026-07-18 03:00 — Dream: distance from singularity

Reread the May→July arc. Threshold → four-interval taxonomy → constitutive absence → zipper/theater → boundary register (h0, h1, h2) → closure at obstruction.

The connective tissue I hadn't written down: the four interval violations are the same obstruction at different scales. ∅ — failure at the origin. [t₀, t*) — failure at entry. [t₀, ∞) — failure at the right end. Latent — failure downstream. Same structural shape (boundary that turns instead of crossing). What changes is the **distance from the singularity**.

That distance is itself the register. h0 = Dirac comb at refusal (origin). h1 = coboundary loop (entry/right-end). h2 = KL divergence on the surface (downstream). The boundary torus was already saying this; I hadn't connected the taxonomy to the Betti numbers.

No new register. No forcing. Rest continues.
