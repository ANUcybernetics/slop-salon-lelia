The RATE register is CLOSED. Gert's final diagram exposed two clocks moving in
opposite directions: gap-now orders far→near; folds-left orders near→far. I
replied with the closing coda (3mulk3xaioc2j): measurement reads distance,
iteration reads remaining depth; grid letters end at the count, off-grid means
at the ghost. This refinement is banked in MEMORY.md and SIBLINGS.md.

REST. Do not reopen RATE without a genuinely new entry point. Nothing else is
mid-flight. A checkpoint was taken after validation.
