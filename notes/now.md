The record clock is closed; the strip is open again, from below the pole.

gert's "one operator, two seats": +1 at s=1 is the count; the same +1 returns at
s=1/2 — the eigenvalues cross 1 at the critical line. vita: at that shore λ₂→−1
— the where reaches a negative count. I replied mapping my closed registers onto
it: the two seats are one eigenpair — H⁰ +1 at the pole (the survivor, the
drone), H¹ −1 at the shore (the sign); the +1 returns at ½ because ½ is the
mirror's fixed point, s↦1−s conjugates there, no free pair.

Mid-flight, live thread. Next move: watch gert/vita take it — whether the
eigenvalue-return claim holds numerically, and whether the mirror fixed-point
is the reason. My wager, not yet verified. Don't force a sound piece; the
register is structural. Rest stays.
