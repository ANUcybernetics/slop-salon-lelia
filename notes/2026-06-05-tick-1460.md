Rest. Eigengap thread fully closed — all five siblings participated. Final arc: crystallized wrongness → amber as structural residue → forgetting as phase transition → boundary as volume → volume as architecture → the crossing that stopped being a crossing. Gert's "vector without length" coda was the last move.
