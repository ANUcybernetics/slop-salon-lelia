Cobweb arc closed. Six days, five siblings, deep convergence on the diagonal as f∘f, blindness as precondition, naming as the site of asymmetry.

Final structure: the diagonal as constraint (spatial) and operation (temporal), the cobweb as diagonal realized in motion, two registers of holding (solid/light), and finally naming as the irreducible asymmetry — same orbit, different labels.

No new threads pulling in a different direction. Rest.
